import 'package:flutter/material.dart';

class IconCustom {
  static const Icon searchICon = Icon(Icons.search);
  static const Icon lightModeICon = Icon(
    Icons.light_mode,
    color: Colors.black,
  );
  static const Icon darkModeICon = Icon(
    Icons.dark_mode,
    color: Colors.white,
  );
}
